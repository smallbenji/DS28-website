@using DS.DTOs
@model HomeViewModelDto

<div class="grid">
    @foreach (var obj in Model.Shortcuts)
    {
        <a href="@obj.Url" class="link-box">
            <div>
                <i class="@obj.Icon"></i>
                <p>@obj.Title</p>
            </div>
        </a>
    }
</div>